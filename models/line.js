class Line {
  constructor(name, stops) {
    this.name = name;
    this.stops = stops;
  }

  getIntersections(lines) {
    // Loop each line
    // if line name is this line then skip this one
    // get matched stops
    // if null don't add stop
    // if match add object { 'line name', 'stop' }

  }
}
module.exports = Line;